<p id="notice"><%= notice %></p>

<%= image_tag(@task.picture_url, :width => 250) if @task.picture.present? %>
<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Date:</strong>
  <%= @task.date %>
</p>

<p>
  <strong>Done:</strong>
  <%= @task.done %>
</p>

<p>
  <strong>Deadline:</strong>
  <%= @task.deadline %>
</p>

<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>
